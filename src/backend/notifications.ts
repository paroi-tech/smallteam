import { Request, Response } from "express"
import { SessionData } from "./session"

export async function routeProcessNotification(subdomain: string, data: any, sessionData?: SessionData, req?: Request, res?: Response) {

}
